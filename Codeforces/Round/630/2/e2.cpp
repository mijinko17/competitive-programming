#include <bits/stdc++.h>
typedef long long lint;
using namespace std;

lint mod = 998244353;
lint n, m, l, r;

struct mat {
    lint a, b, c, d;
    mat(int _a, int _b, int _c, int _d) : a(_a), b(_b), c(_c), d(_d) {}
};

mat mat_pow(mat M, lint k) {
    if (k == 0) {
        return M;
    }
}

int main() {}
