#include <bits/stdc++.h>
#define all(v) v.begin(), v.end()
typedef long long int lint;
using namespace std;

int main() {
    int n;
    string s;
    cin >> n >> s;
    int ans = 0;
    // i:先頭n文字のうちどれを赤にするか
    for (int i = 0; i < (1 << n); i++) {
    }
}
