#include <bits/stdc++.h>
typedef long long lint;
using namespace std;

int main() {
    int s, w;
    cin >> s >> w;
    if (w >= s) {
        cout << "unsafe" << endl;
    } else {
        cout << "safe" << endl;
    }
}
