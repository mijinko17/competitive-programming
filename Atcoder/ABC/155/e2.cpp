#include <bits/stdc++.h>
typedef long long lint;
using namespace std;

int digit_sum(int x) {
    int res = 0;
    string s = to_string(x);
    for (auto e : s) {
        res += e - '0';
    }
}

int main() {
    int n;
    for (int i = 0; i < n + 1000000; i++) {
    }
}
