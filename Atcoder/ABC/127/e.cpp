#include <bits/stdc++.h>
typedef long long lint;
using namespace std;

lint n, m, k;

//距離がdなるマスの組み合わせ
lint num_dist(lint d) {
}

int main() {
    cin >> n >> m >> k;
    if (n > m) {
        swap(n, m);
    }
}
